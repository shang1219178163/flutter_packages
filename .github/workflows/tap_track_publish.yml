name: Publish tap_track

# Package: https://pub.dev/packages/tap_track
# Tag example: tap_track-v1.0.0
on:
  push:
    tags:
      - 'tap_track-v*.*.*'
  workflow_dispatch:

jobs:
  publish:
    permissions:
      id-token: write
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/tap_track

    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Ensure Flutter bin is first in PATH
        run: echo "$HOME/flutter/bin" >> $GITHUB_PATH

      - name: Verify Flutter & Dart
        run: |
          which flutter
          which dart
          flutter --version
          dart --version

      - name: Resolve workspace dependencies
        working-directory: .
        run: dart pub get

      - name: Publish package to pub.dev
        env:
          PUB_CREDENTIALS: ${{ secrets.CREDENTIAL_JSON }}
        run: |
          mkdir -p "$HOME/.config/dart"
          printenv PUB_CREDENTIALS > "$HOME/.config/dart/pub-credentials.json"
          VERSION=$(awk '/^version:/ {print $2; exit}' pubspec.yaml)
          echo "Publishing tap_track $VERSION"
          flutter pub publish --dry-run
          flutter pub publish --force

      - name: Package on pub.dev
        if: always()
        run: |
          {
            echo "## Publish tap_track"
            echo ""
            echo "- Package: [https://pub.dev/packages/tap_track](https://pub.dev/packages/tap_track)"
            echo "- Versions: [https://pub.dev/packages/tap_track/versions](https://pub.dev/packages/tap_track/versions)"
          } >> "$GITHUB_STEP_SUMMARY"
